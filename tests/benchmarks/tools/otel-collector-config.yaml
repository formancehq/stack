receivers:
  otlp:
    protocols:
      grpc:

exporters:
  otlp/uptrace:
    endpoint: otlp.uptrace.dev:4317
    headers:
      uptrace-dsn: 'https://9qJ3nuUHGYeIKr-W4TMN6A@uptrace.dev/1133'
  prometheusremotewrite:
    endpoint: http://prometheus:9090/api/v1/write
    resource_to_telemetry_conversion:
      enabled: true
  logging:

processors:
  batch:

extensions:
  health_check:
  pprof:
    endpoint: :1888
  zpages:
    endpoint: :55679

connectors:
  spanmetrics:
    namespace: span.metrics
    histogram:
      explicit:
        buckets: [ 100us, 1ms, 2ms, 6ms, 10ms, 100ms, 250ms ]
    dimensions:
    - name: http.status_code
    - name: http.method

service:
  telemetry:
    logs:
      level: "debug"
  extensions: [pprof, zpages, health_check]
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [otlp/uptrace, spanmetrics]
    metrics:
      receivers: [otlp, spanmetrics]
      #      processors: [batch]
      exporters: [prometheusremotewrite]
