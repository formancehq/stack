type: 'tool'
language: 'go'

workspace:
    inheritedTasks:
        exclude: ['lint', 'test']

tasks:
    tests:
        command: 'task tests'
        platform: 'system'
        options:
            runInCI: false
