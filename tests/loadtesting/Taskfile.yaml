# https://taskfile.dev

version: '3'

vars:
  LEDGER_COMPOSE_FILE: -f docker-compose.yml

tasks:
  run:
    cmds:
      - docker compose {{.LEDGER_COMPOSE_FILE}} up --abort-on-container-exit --exit-code-from k6
