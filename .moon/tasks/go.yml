# https://moonrepo.dev/docs/config/tasks
$schema: 'https://moonrepo.dev/schemas/tasks.json'

tasks:
  lint:
    command: 'task lint'
    platform: 'system'
    deps:
      - '^:mod'
  mod:
    command: 'go mod tidy'
    platform: 'system'
    deps:
      - 'openapi:build'
