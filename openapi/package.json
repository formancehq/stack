{
  "name": "formance-stack-api",
  "version": "1.0.0",
  "dependencies": {
    "express": "^4.18.2",
    "openapi-merge-cli": "^1.3.1",
    "redoc-express": "^1.0.0"
  },
  "private": true,
  "scripts": {
    "cli": "openapi-merge-cli",
    "build": "npm run cli",
    "build:all": "npm run cli && task -p sdk:generate:all",
    "start": "node main.js"
  }
}
