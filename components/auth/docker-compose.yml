---
version: '3.8'

services:
  dex:
    build:
      context: .
      dockerfile: dex.Dockerfile
    volumes:
      - ./dex-config.yaml:/etc/dex/config.docker.yaml
      - ./pkg/web:/app/web
    ports:
    - '5556:5556/tcp'
    - '8080:8080/tcp'