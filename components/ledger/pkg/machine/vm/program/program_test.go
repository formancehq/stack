package program_test

import (
	"testing"
)

func TestProgram_String(t *testing.T) {
	// p, err := compiler.Compile(`
	// 	send [COIN 99] (
	// 		source = @world
	// 		destination = @alice
	// 	)`)
	// require.NoError(t, err)
	// _ = p.ToString()
}
