---
name: "Credit wallet from payment"
stages:
- send:
    source:
      payment:
        id: "${paymentID}"
    destination:
      wallet:
        id: "${walletID}"
    amount:
      amount: "${amount}"
      asset: "EUR/2"
