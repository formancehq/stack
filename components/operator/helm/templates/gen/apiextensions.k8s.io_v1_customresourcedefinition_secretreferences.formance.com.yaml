apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: secretreferences.formance.com
spec:
  group: formance.com
  names:
    kind: SecretReference
    listKind: SecretReferenceList
    plural: secretreferences
    singular: secretreference
  scope: Cluster
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: SecretReference is the Schema for the secretreferences API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: SecretReferenceSpec defines the desired state of SecretReference
            properties:
              secretName:
                type: string
              stack:
                type: string
            required:
            - secretName
            type: object
          status:
            description: SecretReferenceStatus defines the observed state of SecretReference
            properties:
              hash:
                description: Hash of the secret, allow to watch secrets and reload
                  if needed
                type: string
              info:
                type: string
              ready:
                type: boolean
              syncedSecret:
                description: Synced secret at last reconciliation
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
