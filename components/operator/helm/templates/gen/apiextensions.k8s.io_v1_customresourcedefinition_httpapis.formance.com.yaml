apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: httpapis.formance.com
spec:
  group: formance.com
  names:
    kind: HTTPAPI
    listKind: HTTPAPIList
    plural: httpapis
    singular: httpapi
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: Stack
      jsonPath: .spec.stack
      name: Stack
      type: string
    - description: Ready
      jsonPath: .status.ready
      name: Ready
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: HTTPAPI is the Schema for the HTTPAPIs API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: HTTPAPISpec defines the desired state of HTTPAPI
            properties:
              name:
                description: Name indicates prefix api
                type: string
              rules:
                description: Rules
                items:
                  properties:
                    methods:
                      items:
                        type: string
                      type: array
                    path:
                      type: string
                    secured:
                      type: boolean
                  required:
                  - path
                  type: object
                type: array
              stack:
                type: string
            required:
            - name
            - rules
            type: object
          status:
            description: HTTPAPIStatus defines the observed state of HTTPAPI
            properties:
              info:
                type: string
              ready:
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
