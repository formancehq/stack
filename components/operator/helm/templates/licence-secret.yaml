{{- if (not (eq .Values.operator.licence.secret "")) }}
---
apiVersion: v1
kind: Secret
metadata:
  name: licence
  namespace: formance-system
  labels:
    formance.com/stack: any
stringData:
  token: {{ .Values.operator.licence.token }}
  issuer: {{ .Values.operator.licence.issuer }}
{{- end }}