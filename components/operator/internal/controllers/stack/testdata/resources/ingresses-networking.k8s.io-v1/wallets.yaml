apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
    annotations:
        traefik.ingress.kubernetes.io/router.middlewares: 'stack1-auth-middleware@kubernetescrd, '
    generation: 1
    labels:
        stack: "true"
    name: wallets
    namespace: stack1
spec:
    rules:
        - host: example.net
          http:
            paths:
                - backend:
                    service:
                        name: wallets
                        port:
                            name: http
                  path: /api/wallets
                  pathType: Prefix
status:
    loadBalancer: {}
