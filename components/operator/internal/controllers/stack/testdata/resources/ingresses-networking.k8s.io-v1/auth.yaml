apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
    generation: 1
    labels:
        stack: "true"
    name: auth
    namespace: stack1
spec:
    rules:
        - host: example.net
          http:
            paths:
                - backend:
                    service:
                        name: auth
                        port:
                            name: http
                  path: /api/auth
                  pathType: Prefix
status:
    loadBalancer: {}
