---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: elasticsearchconfigurations.formance.com
spec:
  group: formance.com
  names:
    kind: ElasticSearchConfiguration
    listKind: ElasticSearchConfigurationList
    plural: elasticsearchconfigurations
    singular: elasticsearchconfiguration
  scope: Cluster
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: ElasticSearchConfiguration is the Schema for the elasticsearchconfigs
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ElasticSearchConfigurationSpec defines the desired state
              of ElasticSearchConfiguration
            properties:
              applyOnAllStacks:
                type: boolean
              basicAuth:
                properties:
                  password:
                    type: string
                  secretName:
                    type: string
                  username:
                    type: string
                type: object
              host:
                type: string
              port:
                type: integer
              scheme:
                enum:
                - http
                - https
                type: string
              stacks:
                items:
                  type: string
                type: array
              tls:
                properties:
                  enabled:
                    type: boolean
                  skipCertVerify:
                    type: boolean
                type: object
            required:
            - applyOnAllStacks
            - stacks
            type: object
          status:
            description: ElasticSearchConfigurationStatus defines the observed state
              of ElasticSearchConfiguration
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
