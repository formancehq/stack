// TODO(david): Aliases could be a good genetation idea with a pluralize function, like the one in kubernetes

// Base on the command name, wich is a nouns, we could generate the plural form, and 2 characters form
