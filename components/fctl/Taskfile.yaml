# https://taskfile.dev

version: '3'

tasks:
  default:
    deps: [lint, tests]

  lint:
    cmds:
      - golangci-lint run -v --fix

  tests:
    cmds:
      - go test -race -coverpkg ./... -coverprofile coverage.out ./...
