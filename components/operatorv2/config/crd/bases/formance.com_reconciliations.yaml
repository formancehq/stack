---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: reconciliations.formance.com
spec:
  group: formance.com
  names:
    kind: Reconciliation
    listKind: ReconciliationList
    plural: reconciliations
    singular: reconciliation
  scope: Cluster
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: Reconciliation is the Schema for the reconciliations API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ReconciliationSpec defines the desired state of Reconciliation
            properties:
              debug:
                type: boolean
              dev:
                type: boolean
              resourceProperties:
                properties:
                  limits:
                    properties:
                      cpu:
                        type: string
                      memory:
                        type: string
                    type: object
                  request:
                    properties:
                      cpu:
                        type: string
                      memory:
                        type: string
                    type: object
                type: object
              stack:
                type: string
              version:
                type: string
            required:
            - stack
            type: object
          status:
            description: ReconciliationStatus defines the observed state of Reconciliation
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
