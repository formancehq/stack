type: object
properties:
  type:
    enum:
      - SAVED_METADATA
  log:
    type: object
    properties:
      payload:
        type: object
        required:
          - apply
        properties:
          apply:
            allOf:
              - $ref: "#/definitions/apply"
              - type: object
                properties:
                  log:
                    type: object
                    properties:
                      type:
                        enum:
                          - SET_METADATA
                      data:
                        type: object
                        required:
                          - savedMetadata
                        properties:
                          savedMetadata:
                            $ref: "#/definitions/savedMetadata"
